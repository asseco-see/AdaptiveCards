{
	"id": "rule",
	"version": "0.1",
	"elementPrefix": "",
	"propertyPrefix": "asseco",
	"contributes": {
		"definitions": {
			"Element": {
				"properties": {
					"rules": {
						"type": "Rule[]",
						"required": false,
						"description": "Rule list for element"
					}
				}
			},
			"Rule": {
				"classType": "Class",
				"type": "Rule",
				"properties": {
				   "type": {
					  "type": "RuleType",
					  "required": true,
					  "description": "Rule type"
				   },
				   "trigger": {
					  "type": "TriggerType",
					  "required": true,
					  "description": "Trigger type"
				   },
				   "actions": {
					   "type": "Action[]",
					   "required": true,
					   "description": "Actions to execute on rule"
				   }
				}
			},
			"RuleType": {
				"classType": "Enum",
				"type": "RuleType",
				"default": "Rule.Interaction",
				"values": ["Rule.Interaction"]
			},
			"TriggerType": {
				"description": "Trigger type which can be eaither string or predefined enum values"
			},
			"TriggerTypeString": {
				"type": "string",
				"extends": "TriggerType"
			},
			"TriggerTypeEnum": {
				"type": "string",
				"extends": "TriggerType",
				"enum": ["ValueChanged", "Click", "FocusOut"]
			}
		}
	}
}
